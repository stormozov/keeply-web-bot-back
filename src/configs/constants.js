// =============================================================================
// Константы сервера
// =============================================================================

/**
 * Максимальный размер загружаемого файла (в байтах)
 * @type {number}
 */
export const MAX_FILE_SIZE = 10 * 1024 * 1024; // 10MB
